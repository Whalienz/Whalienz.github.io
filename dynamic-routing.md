# ศัพท์ Dynamic Routing

---

## 📘 Glossary

**Dynamic routing is an addressing method that senses changes in the network topology and responds accordingly without administrator involvement. Dynamic routers:**

- Propagate changes and shifts in the network topology to each router in the network, causing the routing tables on each router to always be up to date.
- Are responsible for all networks to which they are connected.
- Employ additional processes or services to exchange routing information between routers.

---

## 💬 Claude แปลได้ว่า:

Dynamic routing หมายถึง วิธีการกำหนดเส้นทางที่สามารถตรวจจับการเปลี่ยนแปลงในโครงสร้างเครือข่าย และตอบสนองต่อการเปลี่ยนแปลงนั้นได้โดยอัตโนมัติ โดยไม่ต้องมีผู้ดูแลระบบมาจัดการ

**คุณสมบัติของ Dynamic Router:**

- **กระจายข้อมูลการเปลี่ยนแปลง** — เมื่อโครงสร้างเครือข่ายมีการเปลี่ยนแปลง จะส่งข้อมูลไปยังเราเตอร์ทุกตัวในเครือข่าย ทำให้ตารางเส้นทาง (routing table) ของแต่ละเราเตอร์อัปเดตอยู่เสมอ
- **รับผิดชอบเครือข่ายทั้งหมดที่เชื่อมต่ออยู่** — ดูแลและจัดการเส้นทางของทุกเครือข่ายที่ตัวเองเชื่อมต่อ
- **ใช้กระบวนการหรือบริการเพิ่มเติม** — เพื่อแลกเปลี่ยนข้อมูลเส้นทางระหว่างเราเตอร์ด้วยกัน (เช่น โปรโตคอล OSPF, RIP, EIGRP เป็นต้น)

---

## 💬 ChatGPT แปลได้ว่า:

Dynamic routing คือวิธีจัดการเส้นทางเครือข่ายที่สามารถรับรู้การเปลี่ยนแปลงโครงสร้างเครือข่ายได้เอง และปรับตัวโดยไม่ต้องให้ผู้ดูแลระบบเข้าไปแก้ไขด้วยตนเอง โดยมีรายละเอียดดังนี้:

- เมื่อโครงสร้างเครือข่ายมีการเปลี่ยนแปลง เช่น มีการเพิ่มหรือลดอุปกรณ์ เราท์เตอร์ที่ใช้ dynamic routing จะส่งต่อข้อมูลการเปลี่ยนแปลงนี้ไปยังเราท์เตอร์ตัวอื่น ทำให้ตารางเส้นทาง (routing table) ของเราท์เตอร์ทุกตัวอัปเดตอยู่เสมอ
- เราท์เตอร์แบบไดนามิกจะจัดการเครือข่ายทั้งหมดที่เชื่อมต่ออยู่กับตัวมันเอง
- เราท์เตอร์เหล่านี้จะใช้กระบวนการหรือบริการเพิ่มเติมเพื่อแลกเปลี่ยนข้อมูลเส้นทางระหว่างเราท์เตอร์ด้วยกัน

---

## 📝 ความหมายที่เราเข้าใจ:

Dynamic Routing คือระบบที่ทำให้เราเตอร์สามารถปรับเปลี่ยนเส้นทางการส่งข้อมูลได้เองโดยอัตโนมัติ เมื่อมีการเปลี่ยนแปลงในเครือข่าย เช่น มีอุปกรณ์เพิ่มขึ้นหรือขัดข้อง โดยไม่ต้องให้คนมาตั้งค่าใหม่ทุกครั้ง

เราเตอร์จะคุยกันเองผ่านโปรโตคอลต่างๆ (เช่น OSPF, RIP, EIGRP) เพื่ออัปเดตข้อมูลเส้นทางให้ตรงกันตลอดเวลา ทำให้ข้อมูลสามารถเดินทางผ่านเส้นทางที่ดีที่สุดได้เสมอ แม้เครือข่ายจะมีการเปลี่ยนแปลง

---

[← กลับหน้าหลัก](README.md)
